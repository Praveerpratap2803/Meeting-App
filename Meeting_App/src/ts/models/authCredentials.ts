interface IAuthCredentials {
    email:string,
    password:string
}


export default IAuthCredentials // default helps in importing the data with any name in the import statement (used only on when one data is passed)

